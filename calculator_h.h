#include <cmath>
#include <string>
#include <vector>
#include <cctype>
#include <stack>
#include <regex>


bool isExpressionValid(const std::string& str);
double Calculate(const std::string& str);
double CalculateBrackets(std::string str);
